const userSchema = require('./user');
const commentSchema = require('./comment');
const explorationSchema = require('./exploration');

module.exports = {
  userSchema,
  commentSchema,
  explorationSchema
};
